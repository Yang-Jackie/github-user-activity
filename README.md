## Description

A learning project that uses the GitHub REST API to build a command-line interface (CLI) tool.
It fetches and displays a list of public events performed by a given GitHub user.

Built with Python and the GitHub API.

## Usage

```bash
python main.py <username>
```

Example:
```bash
python main.py yang-jackie